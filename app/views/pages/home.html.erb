<div class="home-callout text-center container-fluid">
  <h1>DEVMATCH</h1>
  <h3>ここは地獄です</h3>
</div>

<div class="container home-signup-links">
  <div class="row justify-content-around">
  <!-- 
  <%= link_to "Create your profile!", new_user_profile_path(user_id: current_user.id) if user_signed_in? %>
  -->
    <% if user_signed_in? %>
      <div class="col-md-6 card">
        <div class="well card-body">
          <h2 class="text-center">あなたのプロフィール</h2>
          <% if current_user.profile %>
            <%= link_to "エディット", edit_user_profile_path(user_id: current_user.id), class: "btn btn-default btn-success btn-lg btn-block" %>
            <%= link_to "プロフィール", user_path(id: current_user.id), class: "btn btn-default btn-success btn-lg btn-block" %>
          <% else %>
            <p>コミュニティ向けのプロフィールを作成</p>
            <%= link_to "プロフィール作成", new_user_profile_path(user_id: current_user.id), class: "btn btn-default btn-success btn-lg btn-block" %>
          <% end %>
        </div>
      </div>
      <div> </div>
      <div class="col-md-6 card">
        <div class="well card-body">
          <h2 class="text-center">コミュニティ</h2>
          <p>コミュニティへようこそ</p>
          <%= link_to "コミュニティ", users_path, class: "btn btn-default btn-success btn-lg btn-block"  %>
        </div>

      </div>

    <% else %>
    <div class="col-md-6">
      <div class="card">
      <div class="card-body">
        <h2 class="text-center card-title">ベーシックプラン</h2>
        <h4>基本料無料のコミュニティとなります。</h4>
        <%= link_to "ベーシックプランで登録", new_user_registration_path(plan: @basic_plan.id), class: 'btn btn-primary btn-lg btn-block' %>
      </div>
      </div>
    </div>
    <div class="col-md-6">
      <div class="card">
      <div class="card-body">
        <h2 class="text-center card-title">プロプラン</h2>
        <h4>様々な特典を受けられるコミュニティになります</h4>
        <%= link_to "プロプランで登録", new_user_registration_path(plan: @pro_plan.id), class: 'btn btn-success btn-lg btn-block' %>
      </div>
      </div>
      <% end %>
    </div>
    
  </div>
</div>